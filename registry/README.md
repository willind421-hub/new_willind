# registry

척추 — willind 구조·서비스·스킬·provider의 기준 목록. 새 능력은 여기 등록.

하위: connectors(외부 연결)·services(서비스)·providers(모델/실행)·skills(스킬 등록부)·inputs(입구 라우팅)·kernel(능력 resolver)·knowledge(지식 색인)·permissions(권한 게이트)·hooks(hook 라우팅)·tools·terms·objects·links·files·research·security·data·docs·memory·projects.
